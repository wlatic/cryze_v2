# Cryze Watchdog Configuration
# Copy this to /root/cryze_v2-main/watchdog.env and adjust for your setup.
# All values have sensible defaults in stream_watchdog.sh — only uncomment
# what you need to change.

# --- Network ---
# Gateway IP for the macvlan network (used by ensure_running.sh)
# GATEWAY_IP=10.10.0.1

# Number of expected camera streams
# EXPECTED_STREAMS=2

# --- Timing (seconds) ---
# Grace period after container restart before checking streams
# BOOT_GRACE=180

# How often to check stream health
# CHECK_INTERVAL=60

# Consecutive failures before triggering a restart
# FAIL_THRESHOLD=3

# Base cooldown between restarts (doubles each time, caps at MAX_COOLDOWN)
# BASE_COOLDOWN=300
# MAX_COOLDOWN=3600

# --- Paths ---
# COMPOSE_DIR=/root/cryze_v2-main
# COMPOSE_FILE=docker-compose.macvlan-only.yml
# LOG_FILE=/var/log/cryze-watchdog.log
