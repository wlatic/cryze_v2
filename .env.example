# ===========================================
# Cryze v2 Configuration
# Copy this file to .env and fill in your values.
# ===========================================

# --- Wyze Account (REQUIRED) ---
# Used by both Cryze (GW cameras) and docker-wyze-bridge (non-GW cameras)
WYZE_EMAIL=your@email.com
WYZE_PASSWORD=your_password
# Key ID and API Key from https://developer-api-console.wyze.com/
WYZE_KEY_ID=your_key_id
WYZE_API_KEY=your_api_key

# --- Network Configuration ---
# LAN interface inside this host (run 'ip link' to find yours)
# LXC containers: typically 'eth0'
# VMs / bare metal: typically 'ens18' or similar
LAN_INTERFACE=eth0

# Your LAN subnet and gateway
LAN_SUBNET=10.10.0.0/16
LAN_GATEWAY=10.10.0.1

# Small IP range for containers (must be within LAN_SUBNET, unused by DHCP)
LAN_IP_RANGE=10.10.20.208/29

# Static IPs for containers (must be within LAN_IP_RANGE)
CONTAINER_IP=10.10.20.215   # Android container (Cryze / GW cameras)
API_IP=10.10.20.216          # Cryze API / web UI

# --- Full Stack IPs (docker-compose.frigate.yml only) ---
FRIGATE_IP=10.10.20.210      # Frigate NVR web UI at http://this:5000
WYZE_BRIDGE_IP=10.10.20.211  # Wyze Bridge web UI at http://this:5000

# --- Camera Configuration ---
# Number of GW cameras expected (watchdog uses this)
EXPECTED_STREAMS=2

# --- docker-wyze-bridge (optional) ---
# Stream quality: 1=SD, 2=HD, 3=Auto
WYZE_BRIDGE_QUALITY=2
# Filter cameras (see wyze-bridge docs for filter options)
# FILTER_NAMES=
# FILTER_MODELS=
